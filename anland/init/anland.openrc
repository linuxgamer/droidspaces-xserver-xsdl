#!/sbin/openrc-run
# shellcheck shell=sh disable=SC2034  # vars are consumed by openrc-run

description="Launches DE on anland (Wayland bridge)."

command="/usr/bin/startanland"

depend() {
	need localmount
}
